# Base URL of the TrailQuest backend API (no trailing slash).
VITE_API_URL=http://localhost:4001/api

# Mapbox PUBLIC token (pk.*) — same one used by mobile/.env.
VITE_MAPBOX_TOKEN=pk.your_public_token_here
